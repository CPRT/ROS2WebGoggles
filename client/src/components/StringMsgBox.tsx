export default function StringMessageBox() {
    return (
        <section>
            <h1>String Message Box</h1>
            <p>String message box is a simple React component that fetches a string from the server and displays it in a message box.</p>
        </section>
    );
}